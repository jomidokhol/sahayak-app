<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/gray_bg">

    <!-- Top Header Bar -->
    <LinearLayout
        android:id="@+id/headerBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="88dp"
        android:background="@drawable/header_gradient"
        android:gravity="bottom"
        android:orientation="horizontal"
        android:paddingHorizontal="16dp"
        android:paddingTop="32dp"
        android:paddingBottom="10dp">

        <!-- 1. Left Side: ONLY the Original Logo -->
        <ImageView
            android:id="@+id/ivHeaderLogo"
            android:layout_width="wrap_content"
            android:layout_height="44dp"
            android:adjustViewBounds="true"
            android:src="@drawable/logo"
            android:contentDescription="Logo" />

        <!-- Spacer pushing right-side icons to the far right -->
        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <!-- 2. Right Side Icons -->

        <!-- Golden Crown Badge (Visible in front of Plus icon for Verified Users) -->
        <ImageButton
            android:id="@+id/ivHeaderCrownBadge"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:layout_marginEnd="2dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_crown"
            android:visibility="gone"
            android:contentDescription="Crown Badge" />

        <!-- Plus Action Button -->
        <ImageButton
            android:id="@+id/btnHeaderAddPost"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_add"
            app:tint="@color/white"
            android:visibility="gone"
            android:contentDescription="Add" />

        <!-- Search Button -->
        <ImageButton
            android:id="@+id/btnHeaderSearch"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:layout_marginStart="4dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_search"
            app:tint="@color/white"
            android:contentDescription="Search" />

        <!-- Login Button (Visible when logged out) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnHeaderLogin"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:layout_marginStart="6dp"
            android:backgroundTint="@color/white"
            android:text="লগইন"
            android:textColor="@color/primary_green"
            android:textSize="13sp"
            android:textStyle="bold"
            app:cornerRadius="18dp"
            android:paddingHorizontal="12dp" />

    </LinearLayout>

    <!-- Scrolling Notice Bar -->
    <LinearLayout
        android:id="@+id/noticeBar"
        android:layout_width="match_parent"
        android:layout_height="38dp"
        android:layout_below="@id/headerBar"
        android:background="#2E7D32"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="10dp"
        android:visibility="gone">

        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_notice"
            app:tint="@color/white"
            android:contentDescription="Notice Icon" />

        <com.nur.sahayak.utils.MarqueeTextView
            android:id="@+id/tvNoticeText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:text="লালপুর উপজেলায় আপনাকে স্বাগতম!" />

        <ImageView
            android:id="@+id/btnCloseNotice"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_close"
            app:tint="@color/white"
            android:contentDescription="Close Notice" />

    </LinearLayout>

    <!-- Fragment Container -->
    <FrameLayout
        android:id="@+id/fragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/noticeBar"
        android:layout_above="@id/bottomNavigation" />

    <!-- Freely Draggable Floating Blood Donation Button (Default margin lifted to 32dp) -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardFloatingBlood"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_alignParentEnd="true"
        android:layout_above="@id/bottomNavigation"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="32dp"
        app:cardCornerRadius="28dp"
        app:cardElevation="10dp"
        app:strokeColor="#F42A41"
        app:strokeWidth="2dp"
        app:cardBackgroundColor="#FFFFFF">

        <ImageView
            android:id="@+id/ivFloatingBloodIcon"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="6dp"
            android:scaleType="fitCenter"
            android:src="@drawable/blood"
            android:contentDescription="Blood Donation" />
    </com.google.android.material.card.MaterialCardView>

    <!-- Bottom Navigation Bar with Multi-Colored Icons -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:layout_alignParentBottom="true"
        android:background="@color/white"
        app:elevation="8dp"
        app:itemIconSize="22dp"
        app:itemTextColor="@color/nav_item_color"
        app:labelVisibilityMode="selected"
        app:menu="@menu/bottom_nav_menu" />

</RelativeLayout>
